package cs.ucla.edu.bwaspark.datatype

class SWAlnType {
  var score: Int = 0           // best score
  var tEnd: Int = -1           // target end
  var qEnd: Int = -1           // query end
  var scoreSecond: Int = -1    // second best score
  var tEndSecond: Int = -1     // second best ending position on the target
  var tBeg: Int = -1           // target start
  var qBeg: Int = -1           // query start
}

